<%= form_for(@tiposeguimiento) do |f| %>
  <% if @tiposeguimiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tiposeguimiento.errors.count, "error") %> prohibited this tiposeguimiento from being saved:</h2>

      <ul>
      <% @tiposeguimiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :activo %><br>
    <%= f.check_box :activo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
